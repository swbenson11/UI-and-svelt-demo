What I've done so far.
  // use degit to install a sveltjs template
npx degit sveltejs/template svelte-app
npm i swipejs
npm install
npm run dev

# Install babel
npm i -D @babel/core @babel/preset-env rollup-plugin-babel core-js
# install sass
npm i -D  rollup-plugin-sass
# install auto prefixer
npm i -D autoprefixer

  // add babel.config.js
  // modify rollup.config.js



Once you've tinkered a bit and understood how everything fits together, you can fork  and start doing this instead:
npx degit your-name/template my-new-project

This should be using rollup
npm run build
   to create a production-ready version of your app, and check the project template's  for instructions on how to easily deploy your app to the web with Now or Surge.



   Get a list of your browser support
   npx browserslist

npm install -D svelte-preprocess



Remove:
npm install -D node-sass
npm i -D  rollup-plugin-sass
npm install --save-dev svelte-preprocess-sass node-sass

npm install -D rollup-plugin-css-only
npm i -D rollup-plugin-postcss